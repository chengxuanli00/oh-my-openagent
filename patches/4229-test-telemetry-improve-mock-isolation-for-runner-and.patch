From db5beef015bd8f9c5cef53e95f700b5a038c0949 Mon Sep 17 00:00:00 2001
From: YeonGyu-Kim <code.yeon.gyu@gmail.com>
Date: Sat, 11 Apr 2026 22:14:44 +0900
Subject: [PATCH 4229/5749] test(telemetry): improve mock isolation for runner
 and index tests

---
 src/cli/run/runner.telemetry.test.ts | 13 +++----------
 src/index.telemetry.test.ts          |  9 ++++++++-
 2 files changed, 11 insertions(+), 11 deletions(-)

diff --git a/src/cli/run/runner.telemetry.test.ts b/src/cli/run/runner.telemetry.test.ts
index 0117f74a..3d4b5193 100644
--- a/src/cli/run/runner.telemetry.test.ts
+++ b/src/cli/run/runner.telemetry.test.ts
@@ -1,5 +1,7 @@
 import { afterEach, describe, expect, it, mock } from "bun:test"
 
+async function* createEmptyEventStream(): AsyncIterable<unknown> {}
+
 describe("run telemetry isolation", () => {
   afterEach(() => {
     mock.restore()
@@ -13,20 +15,11 @@ describe("run telemetry isolation", () => {
     mock.module("./agent-resolver", () => ({
       resolveRunAgent: mock(() => "Sisyphus - Ultraworker"),
     }))
-    mock.module("./events", () => ({
-      createEventState: mock(() => ({
-        messageCount: 0,
-        lastPartText: "Run completed",
-        agentColorsByName: {},
-      })),
-      processEvents: mock(async () => {}),
-      serializeError: (error: unknown) => (error instanceof Error ? error.message : String(error)),
-    }))
     mock.module("./server-connection", () => ({
       createServerConnection: mock(async () => ({
         client: {
           event: {
-            subscribe: mock(async () => ({ stream: {} })),
+            subscribe: mock(async () => ({ stream: createEmptyEventStream() })),
           },
           session: {
             promptAsync: mock(async () => undefined),
diff --git a/src/index.telemetry.test.ts b/src/index.telemetry.test.ts
index 2d554626..f3026a1c 100644
--- a/src/index.telemetry.test.ts
+++ b/src/index.telemetry.test.ts
@@ -95,7 +95,14 @@ function installModuleMocks(): void {
     startBackgroundCheck: mock(() => {}),
   }))
   mock.module("./tools/lsp/client", () => ({
-    lspManager: {},
+    lspManager: {
+      getClient: mock(async () => ({
+        diagnostics: mock(async () => ({ items: [] })),
+      })),
+      stopAll: mock(async () => {}),
+      releaseClient: mock(() => {}),
+      cleanupTempDirectoryClients: mock(async () => {}),
+    },
   }))
   mock.module("./shared/posthog", () => ({
     createPluginPostHog: mockCreatePluginPostHog,
-- 
2.39.2

